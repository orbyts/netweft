# Deploy SSH client configuration

Render for a named client, inspect the generated SSH config, copy it to that client if rendered elsewhere, and include it from `~/.ssh/config`. Never copy private keys as part of the generated artifact.
