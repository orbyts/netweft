# Adapter SDK

Adapters consume `ResolvedPlan`, render deterministic files, own distinct output trees, keep secrets out of generated artifacts, identify target hosts, and never deploy during render.

Current built-ins:

```text
bind
env
nginx
```
