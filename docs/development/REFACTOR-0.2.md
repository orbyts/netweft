# Netweft 0.2 refactor

```text
configuration → validation and resolution → ResolvedPlan → adapters → artifacts
```

Compatibility commands include BIND, environment, proxy inspection, and native Nginx rendering. Deployment remains outside Netweft.
